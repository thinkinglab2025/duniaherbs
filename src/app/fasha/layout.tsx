import type { ReactNode } from 'react';

/**
 * Fasha landing page — layout berasingan dari HQ.
 * Tiada ChatBot — fokus penuh pada experience glamour.
 */
export default function FashaLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
